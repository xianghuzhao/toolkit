# DIRAC Server Maintenance Tools

## MySQL

Before running the tools, make sure the password in the `auth` file is
correct. Copy the `auth.example` file and edit:

    $ cp mysql/auth.example mysql/auth
    $ vim mysql/auth
